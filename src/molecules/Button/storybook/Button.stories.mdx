import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import { colorListArray } from 'atoms/colors/colors';

import Button from 'molecules/Button/Button';
import * as stories from 'molecules/Button/storybook/Button.stories.tsx';

import { iconMap } from 'utils/storyUtils.stories.tsx';

<Meta
  title="Components/Actions/Buttons"
  component={Button}
  argTypes={{
    size: {
      options: ['xlarge', 'large', 'medium', 'small', 'xsmall'],
      control: { type: 'radio' },
    },
    color: {
      options: ['primary', 'secondary', 'success', 'purple', 'magenta', 'black'],
      control: { type: 'radio' },
    },
    borderColor: {
      options: [...colorListArray, undefined],
      control: { type: 'select' },
    },
    textColor: {
      options: [...colorListArray, undefined],
      control: { type: 'select' },
    },
    backgroundColor: {
      options: [...colorListArray, undefined],
      control: { type: 'select' },
    },
    disabled: {
      control: { type: 'boolean' },
    },
    children: {
      control: { type: 'text' },
      description: 'Text or JSX element',
    },
    endIcon: {
      options: Object.keys(iconMap),
      mapping: iconMap,
      control: { type: 'select' },
    },
    startIcon: {
      options: Object.keys(iconMap),
      mapping: iconMap,
      control: { type: 'select' },
    },
  }}
/>

# Button

Buttons allow users to take actions, and make choices, with a single tap. Buttons communicate actions that users can
take. They are typically placed throughout your UI, in places like:

- Modal windows
- Forms
- Cards
- Toolbars

Contained buttons are high-emphasis, distinguished by their use of elevation and fill. They contain actions that are
primary to your app.

## Usage

Using the Contained Button is as easy as writing the word Button with your text as the child.

```jsx
<Button>This is a contained button</Button>
```

#### Adding Icons

You can pass any ReactNode into the endIcon or startIcon props to be rendered at the end or beginning of your Button.

```jsx
<Button endIcon={<FiCoffee />}>Coffee Time!</Button>
```

## Additonal Props

<Canvas>
  <Story name="Buttons" story={stories.contained} />
</Canvas>

<ArgsTable story="Buttons" />
